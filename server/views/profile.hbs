<h2>{{friend.name}}'s emoji {{#if viewingOwnPage}}<span class="note">(it's you!){{myName}}</span>{{/if}}</h2>

<div class="bubble-wrapper">
	{{#each friend.receivedMessages}}<span class="emoji-bubble">{{this.emojiMessage}}<span>From: {{this.from.name}},<br>{{this.timeStamp}}</span></span>{{/each}}
</div>

{{#if viewingOwnPage}}
	<p>You can't post on your own page :/</p>
{{else}}
	<input id="emoji-field" placeholder="emoji" />
	<button type="submit" id="send-emoji">Submit</button>
	<button type="submit" id="random-emoji">Send a random emoji!</button>
{{/if}}